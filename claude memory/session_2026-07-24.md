# Session handoff — 2026-07-24

In-repo notes from a working session (companion to Claude's private memory).
Branch: `sph-use-case`.

## What this session did

Two threads: (A) re-understanding + tidying the code, (B) a new `vibecoding/`
visualization. Plus one repo-wide rename.

### A. Code re-understanding & comment cleanup

- **Re-read the whole linear pipeline** (datagen → model12 → rdsph_loss(λ3·|KG|²)
  → trainer → `model12_sph_l4.pt` → WholeCloudCorrector2D → experiments) against
  the actual source (not docs). No logic changes.
- **`src/training/datagen.py`** — comment-only cleanup:
  - trimmed the verbose `periodic=True` block; removed a stale `make_invariant`
    reference (that fn was deleted from this branch with model9).
  - de-scoped SPH-specific wording where the mechanism is generic (periodic
    boundary conditions / minimum-image is not SPH-specific).
  - removed cryptic "(lattice vs dart throwing)" aside; added clear step-by-step
    comments in `generate_sample`, `_periodic_cloud`, `noise_sample`.
- **`src/models/architectures/model12/model12.py`** — added inline comments
  explaining the message-passing architecture (edge/node/out MLP roles, fixed
  proximity kernel, per-round geometry, residual updates, gather/scatter in
  `forward_sparse`). Removed the "Why it exists —" docstring heading (LLM talk).

### B. New `vibecoding/` sandbox (non-critical, disposable prototype code)

- `vibecoding/README.md` — describes the sandbox; structure = visualizations/,
  sweeps/, misc/.
- `vibecoding/visualizations/datagen_stages.py` — the deliverable. 5-panel figure
  of the **data-generation process only** (no model/correction):
  `1. Lattice → 2. +jitter → 3. Torus placement (clean) → 4. +noise (noisy)
  → 5. Unwrapped to 2D (noisy)`.
  - Pre-torus stages drawn flat; on-torus stages (3,4) drawn on an **actual 3D
    torus** (isometric orthographic view, surface graticule grid, points
    size/opacity-cued by camera depth). Panel 5 unwraps the noisy cloud back to
    the flat box (with periodic ghosts) to show the torus IS the periodic square.
  - Points colored by nearest-neighbour distance (shared scale, viridis).
  - Reproduces `_periodic_cloud`/`noise_sample` exactly (lattice params read off
    a real `PoissonDiskDataset`). Output: `visualizations/outputs/datagen_stages.png`.
  - Run: `.venv\Scripts\python.exe vibecoding/visualizations/datagen_stages.py`
- (An earlier `torus_3d.py` was created then removed — its donuts got folded into
  datagen_stages.py.)

### C. Repo-wide rename: `attention_rd` → `cutoff_rd`

The word "attention" wrongly implied a transformer; the mechanism is a FIXED
proximity kernel with a compact-support cutoff. Renamed the config key and
purged the word "attention" everywhere (code, 3 configs, CLAUDE.md, README.md,
weights/README.md). Prose "attention" → "proximity kernel" / "cutoff".
**Verified:** `tests/test_wholecloud.py` bit-exact PASS; smoke trainer logs
`Cutoff rd = 0.286`; grep for "attention" → nothing.

## Preferences established this session (also in Claude's private memory)

- **De-scope by removal**: when something needn't be X-specific, DELETE the
  scoping wording — don't add a "not X-specific" note.
- **No LLM talk**: no self-narrating meta-phrases in comments/docstrings
  ("Why it exists", "The key idea", "Note that") — state the fact directly.
- **Verify against artifacts**, not docs (docs drift).

## State / possible next steps

- All changes are comment/rename/new-file only — no behavior change; the
  production pipeline and checkpoint are untouched. Nothing committed yet.
- model9 dataset process = `(clean, noisy)` pairs only; "back to lattice" is the
  model (inference), not the dataset — kept out of the datagen figure by design.
- Open ideas from earlier (not started): 3D model12 (needs 3D KG in
  utils/metrics.py), λ3 ablations incl. the missing λ3=0 control, paper figures.
- If you want the `cutoff_rd` name changed (e.g. `kernel_rd`), it's a quick
  find-replace.
