# pbc_toy

Standalone synthetic proof that the tiling + ghost-buffer approach is correct
under periodic boundaries: plants violations on a small cloud, corrects it
tile by tile, and shows no seam artifacts. No SPH data needed.

```bash
.venv\Scripts\python.exe src/inference/experiments/pbc_toy/pbc_toy.py
```
